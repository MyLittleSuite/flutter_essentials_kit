name: Dart CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Install dependencies
        run: ./flutterw pub get
      - name: Run tests
        run: ./flutterw test
